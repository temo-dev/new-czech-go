# Add this package to dev_dependencies first:
# flutter pub add flutter_launcher_icons --dev

flutter_launcher_icons:
  android: false
  ios: true
  image_path: "assets/icon/app_icon.png"
  remove_alpha_ios: true
